<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        @font-face {
            font-family: 'Khmer OS Siemreap Regular';
            src: url({{asset "/invoice-pos/assets/Khmer OS Siemreap Regular.ttf" "dataURI"}});
            format('ttf');
        }
        {{asset "/invoice-pos/assets/invoice-style.css"}}
    </style>
</head>
<body>
    <div class="content">
        {{#each data}}
        <header class="header">
            <div class="top-header">
                <p>លេខវិក្ក័យប័ត្រ <span>#{{receipt_number}}</span></p>
            </div>
            <div class="buttom-header">
                <div class="box">
                    <p>បង្កើតនៅៈ {{ordered_at}}</p>
                </div>
                <div class="box right">
                    <p>អ្នកគិតលុយៈ {{cashier.name}}</p>
                </div>
            </div>
        </header>
        <main>
            <table class="table">
                <tr class="table-header">
                    <td>ឈ.រ</td>
                    <td>ផលិតផល</td>
                    <td>តម្លៃ (រៀល)</td>
                    <td>ចំនួន</td>
                    <td>សរុប</td>
                </tr>
                <tbody class="table-body">
                    {{#each details}}
                    <tr class="item">
                        <td>{{increment}}</td>
                        <td>
                            {{product.name}}
                        </td>
                        <td>
                            {{unit_price}}
                        </td>
                        <td>
                            {{qty}}
                        </td>
                        <td>
                            {{{subtotal unit_price qty}}}
                        </td>
                    </tr>
                    {{/each}}
                    <tr>
                        <td colspan="4" class="td"><span>តម្លៃសរុប (រៀល):</span></td>
                        <td><span class="text-green">{{total_price}}</span></td>
                    </tr>
                </tbody>
            </table>
        </main>
        {{/each}}
    </div>
</body>
</html>